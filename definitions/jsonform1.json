{
    "type": "object",
    "properties": {
        "bbox": {
            "type": "spatial",
            "title": "Select area on map",
            "options": {
                "for": "eox-map#main"
            },
            "format": "bounding-box"
        },
        "date": {
            "title": "Select date",
            "type": "date",
            "format": "date"
        },
        "threshold": {
            "title": "Select threshold",
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "step": 0.05,
            "default": 0.55,
            "format": "range"
        }
    }
}